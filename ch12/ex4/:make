#include "Stack.h"
#include <iostream>
#include <cctype>
#include <ctime>
#include <cstdlib>

using std::srand;
using std::rand;
using std::time;
using std::cout;
using std::cin;

int main()
{
    srand(time(0));
    Stack st1(5);

    for (int i = 0; i < 5; i++) {
        st1.push(rand());
    }

    cout << st1 << '\n';

 //   Stack st2(11);

 //   cout << st2 << '\n';
}
